language: python
python:
  - 3.3
  - 3.4
  - 3.5
sudo: false

install:
  - pip install codecov coveralls

script:
  - nosetests --with-coverage

# Calculate coverage
after_success:
  - coveralls
  - codecov

notifications:
  email:
    - brendan@bitsmithy.net
